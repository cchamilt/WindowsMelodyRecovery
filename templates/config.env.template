# Email Notification Settings
BACKUP_EMAIL_FROM="your-email@domain.com"
BACKUP_EMAIL_TO="your-email@domain.com"
BACKUP_EMAIL_PASSWORD="your-app-password"

# SMTP Settings (optional - defaults to Office 365)
SMTP_SERVER="smtp.office365.com"
SMTP_PORT="587"

# Git Settings (optional)
GIT_USER_NAME="Your Name"
GIT_USER_EMAIL="your-email@domain.com"
GIT_DEFAULT_BRANCH="main"

# Other custom environment variables
# CUSTOM_VAR="custom-value" 